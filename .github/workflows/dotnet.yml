# This workflow will build a .NET project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

name: .NET

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: 6.0.x
    - name: Restore dependencies
      run: dotnet restore
    - name: Build
      run: dotnet build --no-restore -c Release;find
    ##- name: Test
    #  run: dotnet test --no-build --verbosity normal
    - name: Archive production artifacts Jellyfin.Plugin.Imdb
      uses: actions/upload-artifact@v3
      with:
        name: Jellyfin.Plugin.Imdb.dll (net6)
        if-no-files-found: error # 'warn' or 'ignore' are also available, defaults to `warn`
        path: |
          Jellyfin.Plugin.Imdb/obj/Release/net6.0/Jellyfin.Plugin.Imdb.dll
    - name: Archive production artifacts Jellyfin.Plugin.NfoDateCreated
      uses: actions/upload-artifact@v3
      with:
        name: Jellyfin.Plugin.NfoDateCreated.dll (net6)
        if-no-files-found: error # 'warn' or 'ignore' are also available, defaults to `warn`
        path: |
          Jellyfin.Plugin.NfoDateCreated/obj/Release/net6.0/Jellyfin.Plugin.NfoDateCreated.dll